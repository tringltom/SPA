import { EkvitiColors } from "./EkvitiColors";

const Ancor = {
  cursor: "pointer",
  paddingLeft: 5,
  textDecoration: "underline",
  color: EkvitiColors.primary,
};

const styles = {
  Ancor,
};

export {styles};